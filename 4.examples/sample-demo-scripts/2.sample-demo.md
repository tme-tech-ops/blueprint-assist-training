# Blueprint Assist Demo Script

- Audience: Blueprint authors, solution architects
- Topics: Spec-driven development, AI-assisted authoring, constitution compliance, validation
- Key Skill: Generating a validated blueprint from a natural language specification

## Session Setup

- **Session Type**: Fresh session
- **Bypass Mode**: Enabled

## Demo Steps - Demo 2

### 2.1 Show the constitution file

```text
Show me the constitution file for blueprint authoring from the spec-kit reference 
.\3.resources\spec-kit\.specify\memory\constitution-template-blueprint.md
```

### 2.2 Review the consolidated spec

Before we build the blueprint, let's review the consolidated spec to understand what we're building. The spec does reference a Dell Automation Studio template that we have pre-downloaded. File located at C:\temp\VMaaS_for_vSphere-1.0.0.0.zip

```text
Show me how dap-bpa will use a blueprint specfication
and outline the steps to build a blueprint from that spec
at 4.examples/sample-specs/001-spec-plan-blueprint/spec-consolidated.md
```

### 2.3 Build blueprint from specification

Using the consolidated spec, build out the blueprint and place in the planned location target build folder referenced in the spec:

```text
Build out the blueprint from this spec and place in the planned location target build folder referenced in the spec:

Here is the path: 4.examples/sample-specs/001-spec-plan-blueprint/spec-consolidated.md
```

### 2.4 Show the generated blueprint structure

```text
Show me the directory structure and files that were generated for the blueprint
```

### 2.5 Lint the blueprint

```bash
dap-bpa blueprint lint --file 4.examples/target-build-folder/WindowsVM_Vsphere_demo/blueprint.yaml --verify
```

### 2.6 Validate the blueprint

```bash
dap-bpa blueprint validate-all --file 4.examples/target-build-folder/WindowsVM_Vsphere_demo/blueprint.yaml
```

### 2.7 Verify constitution compliance

```text
Review the generated blueprint and confirm it is compliant with the constitution — check for secrets handling, input structure, proper node types, and file layout
```

## Optional Demo Steps

### 2.8 Blueprint analysis

```text
Analyze the blueprint in 4.examples/target-build-folder/WindowsVM_Vsphere_demo and summarise its node types, inputs, and any issues.
```

---

## Notes

- Ensure the sample spec files exist at the referenced paths before running
- Bypass mode should be enabled for unrestricted access
- The blueprint output folder is determined by the spec — confirm the path before the demo
- Linting must pass before moving to Demo 3
